# =========================================================
#      PROYECTO SGC - CONFIGURACIÓN PRODUCCIÓN (.env.prod)
# =========================================================
# Archivo de variables de entorno para producción. Completa los valores según tu entorno seguro.

# --- Configuración General de Docker ---
FRONTEND_PORT=
BACKEND_PORT=

# --- Credenciales de la Base de Datos (para 'db' y 'backend') ---
DB_ROOT_PASSWORD=
DB_NAME=
DB_USER_APP=
DB_ADMIN_APP=
DB_USER_PASSWORD=
DB_ADMIN_PASSWORD=
DB_HOST=
DB_PORT=

# --- Configuración del Backend (para 'backend') ---
JWT_ALGORITHM=
ACCESS_TOKEN_EXPIRE_MINUTES=

# --- CORS del Backend (para 'backend') ---
ALLOWED_ORIGINS=

# --- Configuración del Frontend (para 'frontend') ---
VITE_API_BASE_URL=

# --- Configuración de Email (para notificaciones y recuperación de contraseña) ---
MAIL_SERVER=
MAIL_PORT=
MAIL_USE_TLS=
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_DEFAULT_SENDER=

# --- Configuración de Logging ---
LOG_PATH=
LOG_MAX_SIZE_MB=
LOG_BACKUP_COUNT=

# --- Configuración de Seguridad adicional ---
PASSWORD_HASH_ALGORITHM=
PASSWORD_MIN_LENGTH=

# --- Token de acceso para APIs externas (si aplica) ---
# EXTERNAL_API_TOKEN=

# --- Variables para pruebas automáticas (e2e, CI/CD) ---
TEST_USER_EMAIL=
TEST_USER_PASSWORD=
